import React from 'react'
import { Space } from 'antd';

function PageFunctions({ children }) {
  return (
    <div className="site-page-functions">
      {children}
    </div>
  )
}

export default PageFunctions;